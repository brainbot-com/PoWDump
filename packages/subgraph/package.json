{
  "name": "subgraph",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dump-pow",
    "create-local": "graph create --node http://localhost:8020/ dump-pow",
    "remove-local": "graph remove --node http://localhost:8020/ dump-pow",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dump-pow"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.31.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
